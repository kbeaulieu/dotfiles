# ~/.tmuxinator/kronos.yml

name: Kronos
root: ~/kronos/projects
windows:
  - dalake:
      root: ~/kronos/projects/dalake
      panes:
        - # ./gradlew dependencyUpdates
  - dabench:
      root: ~/kronos/projects/dabench
      panes:
        - git status -sb; cd web; yarn outdated
        - cd backend; # ./gradlew dependencyUpdates
  - ff:
      root: ~/kronos/projects/fullfinance
      panes:
        - git status -sb; yarn outdated
  - std:
      root: ~/kronos/projects/standards
      panes:
        - git status -sb
